Run the command below to install the libraries in your requirements.txt

pip install requirements.txt

To execute the code

python app.py

(It may Takes too long to generate an image.)
(you can use smaller diffusion model that runs perfectly on your system, most of the diffusion model needs requires gpu , you can use models like runwayml/stable-diffusion-v1-5 for low end systems)
